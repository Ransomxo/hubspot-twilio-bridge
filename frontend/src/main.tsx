import React from 'react';
import ReactDOM from 'react-dom/client';
import SmsComposerPage from './pages/sms-composer';
import './styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SmsComposerPage />
  </React.StrictMode>
);
